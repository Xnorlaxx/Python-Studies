def get_positions_of_char(another_string, char_being_searched_for):
    result = ""
    for position, char in enumerate(another_string):
        if char == char_being_searched_for:
            result += str(position) + ','
    return result